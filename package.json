{
  "name": "edge-link-router-dev",
  "private": true,
  "scripts": {
    "env:start": "wp-env start",
    "env:stop": "wp-env stop",
    "env:destroy": "wp-env destroy",
    "env:logs": "wp-env logs",
    "wp:cli": "wp-env run cli wp",
    "wp:plugin:activate": "wp-env run cli wp plugin activate edge-link-router",
    "wp:plugin:deactivate": "wp-env run cli wp plugin deactivate edge-link-router",
    "wp:rewrite:flush": "wp-env run cli wp rewrite flush",
    "wp:db:reset": "wp-env run cli wp db reset --yes",
    "wp:db:export": "wp-env run cli wp db export /var/www/html/wp-content/plugins/edge-link-router/dump.sql",
    "lint:php": "composer run phpcs",
    "lint:php:fix": "composer run phpcbf",
    "check:plugin": "wp-env run cli wp plugin check plugin --slug=edge-link-router --exclude-directories=build --fields=file,line,column,type,code,message"
  },
  "devDependencies": {
    "@wordpress/env": "^10.0.0",
    "puppeteer": "^24.37.5"
  }
}
